{
  "name": "extension",
  "version": "0.1.0",
  "private": true,
  "description": "Fluent browser extension — highlight-to-translate on any page.",
  "scripts": {
    "build": "node build.mjs",
    "icons": "node scripts/generate-icons.mjs",
    "typecheck": "tsc --noEmit -p ."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "esbuild": "^0.24.0",
    "sharp": "^0.33.5",
    "typescript": "5.9.3"
  }
}
